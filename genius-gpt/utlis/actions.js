// 'use server';

// export const generateChatResponse = async (chatMessage) => {
//   console.log(chatMessage);
//   return 'awesome';
// };

'use server';

export const generateChatResponse = async (chatMessage) => {
  console.log(chatMessage);
  return 'awesome';
};
